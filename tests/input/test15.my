def decrement(x):
  return x - 1
enddef

def increment(x):
  return x + 1
enddef

def compute(x,y) :
  sum = y
  while x > 0:
    sum = increment(sum)
    x = decrement(x)
  endwhile
  return sum
enddef

print (compute(compute(10,20),compute(3,2)))




